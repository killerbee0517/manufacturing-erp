spring:
  datasource:
    url: jdbc:h2:mem:erp;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

app:
  jwt:
    issuer: test
    secret: test-secret-test-secret-test-secret
    expiresMinutes: 60
